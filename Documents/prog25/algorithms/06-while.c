#include <stdio.h>

int main()
{
    // inicializa um indice (i) com zero
    int i = 0;
    // enquanto i < 10
    while (i < 10)
    {
        // mostre o número i
        printf("i = %i\n", i);
        // incremente i
        i = i + 1;
    }
    return 0;
}
